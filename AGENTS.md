# AGENTS.md

The guidance for coding agents in this repository lives in [`CLAUDE.md`](CLAUDE.md) — read it
before making changes. It is kept in one file so the two can't drift apart again: this file used to
be a copy, and by the time it was replaced it still described loop-only routes, a single Mapbox
caller and none of the Google Places pipeline.
