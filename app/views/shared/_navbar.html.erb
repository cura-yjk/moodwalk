<nav class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <div class="container-fluid">

        <div class="d-flex flex-column align-items-center">
          <div>
            <%= link_to walks_path, class: "nav-link" do %>
              <i class="fa-solid fa-map-location-dot nav-icon
              <%= active_icon_class(walks_path) %>
              <%= 'nav-active-highlight' if current_page?(walks_path) %>"
              ></i>
            <% end %>
          </div>

          <div class="nav-title <%= active_icon_class(walks_path) %> <%= 'nav-active-highlight' if current_page?(walks_path) %>">
            History
          </div>
        </div>

        <div class="d-flex flex-column align-items-center">
          <div>
            <%= link_to root_path, class: "nav-link" do %>
            <i class="fa-solid fa-leaf nav-icon
              <%= active_icon_class(root_path) %>
              <%= 'nav-active-highlight' if current_page?(root_path) %>"
            ></i>
            <% end %>
          </div>

          <div class="nav-title <%= active_icon_class(root_path) %> <%= 'nav-active-highlight' if current_page?(root_path) %>">
            Explore
          </div>
        </div>

        <div class="d-flex flex-column align-items-center">
          <div>
            <%= link_to community_routes_path, class: "nav-link" do %>
              <i class="fa-solid fa-users nav-icon
              <%= active_icon_class(community_routes_path) %>
              <%= 'nav-active-highlight' if current_page?(community_routes_path) %>"
              ></i>
            <% end %>
          </div>

          <div class="nav-title  <%= active_icon_class(community_routes_path) %> <%= 'nav-active-highlight' if current_page?(community_routes_path) %>">
            Paths
          </div>
        </div>

    </div>
  </div>
</nav>
